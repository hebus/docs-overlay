---
title: Setup
description: Never touched again, so every version inherits this exact file.
---

No version overrides this page. Open it under 4.0.0 and you are reading this very file, three hops up
the inheritance chain — one HTML page per version, all compiled from one source.
