# Everything below is generated by `docs-overlay materialize` from content/docs/.
# Edit content/docs/ instead; these are rewritten on every build.
#
# Anchored with a leading slash, every one of them. An unanchored `docs/` would also match
# `content/docs/` and hide every source file in this example — a failure that looks like a
# disappearance rather than an error.
/docs/
/versioned_docs/
/versioned_sidebars/
/versions.json
/.docs-overlay/

# Docusaurus
/build/
/.docusaurus/
/node_modules/
