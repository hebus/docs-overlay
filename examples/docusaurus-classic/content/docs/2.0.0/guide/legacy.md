---
title: Legacy behaviour
overlay:
  deleted: true
  replacedBy: guide/new-api
---

This body is never served. The file exists so that the version which removes the page is the version
that says so, and so the deletion is derived from this file's own path.
